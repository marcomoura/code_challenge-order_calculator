require 'spec_helper'

describe OrderCalculator do
  it 'has a version number' do
    expect(OrderCalculator::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
